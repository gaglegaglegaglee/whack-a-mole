# 두더지잡기 PRD

> 이 문서는 두더지잡기에서 무엇을 만들지 계속 갱신하는 개발 지시서다.  
> 최종 수정일: 2026-09-14

## 1. 한 줄 요약

60초 동안 무작위로 나타나는 두더지를 터치해 잡고, 10마리마다 판이 3×3에서 최대 5×5까지 커지며 동시 두더지도 1마리에서 최대 3마리까지 늘어나는 모바일 우선 웹게임이다.

## 2. 왜 만드는가

규칙을 배우는 데 시간이 들지 않으면서 짧은 시간에 손맛과 긴장감을 느낄 수 있는 게임을 만든다. 단순한 무작위 연타가 최선이 되지 않도록 빈 구멍 오답에는 감점을 적용하고, 단계가 오를수록 판의 크기와 속도가 함께 증가하게 한다.

## 3. 누가 쓰는가

- 설치나 로그인 없이 휴대폰에서 짧게 게임을 즐기려는 한 명의 사용자
- 손가락 터치를 주로 사용하지만 PC에서는 마우스로도 같은 규칙을 플레이한다.
- 진동 지원 기기에서는 터치와 적중의 차이를 촉각으로 느낀다.

## 4. 핵심 기능 목록과 우선순위

### 반드시 있어야 함(MVP)

- **F1. 시작과 게임판**: 시작 화면, 3초 카운트다운, 단계에 맞는 구멍 판을 제공한다.
- **F2. 두더지 등장과 판정**: 단계별로 최대 1·2·3마리의 두더지가 무작위 구멍에 나타나고 터치 결과를 각각 판정한다.
- **F3. 점수와 단계 상승**: 적중·오답 점수와 10마리 단위 단계 상승 및 속도 변화를 처리한다.
- **F4. 시간과 결과**: 60초 타이머, 종료 화면, 다시 하기를 제공한다.
- **F5. 타격 피드백과 기기 저장**: 망치·적중 애니메이션, 진동·효과음·음소거와 최고 기록 저장을 제공한다.

### 있으면 좋음

- **F6. 안전한 일시정지**: 화면이 가려지면 자동으로 멈추고 사용자가 직접 재개한다.

### 나중에

- 황금 두더지, 폭탄 두더지 등 특수 두더지
- 아이템, 콤보 배수, 스테이지 모드
- 온라인 순위, 로그인, 여러 기기 기록 동기화

## 5. 필수 기능별 상세

### F1. 시작과 게임판

사용자는 첫 화면에서 간단한 규칙을 읽고 `게임 시작`을 누른다. 화면 중앙에 3, 2, 1 카운트다운을 표시한 뒤 60초 게임을 시작한다. 첫 단계는 3×3 구멍 판이며 밝고 코믹한 아케이드 스타일을 사용한다.

완료 기준:

- [ ] 시작 전에는 점수와 시간이 줄어들지 않는다.
- [ ] 게임 시작 버튼을 누르면 3초 카운트다운 후 첫 두더지가 나타난다.
- [ ] 320px 너비의 세로 화면에서도 모든 구멍과 상태 정보가 잘리지 않는다.
- [ ] 휴대폰 터치와 PC 마우스 클릭이 같은 결과를 낸다.
- [ ] 빠르게 여러 번 시작 버튼을 눌러도 게임은 한 판만 시작된다.

예외 상황:

- 카운트다운 도중 화면이 가려지면 안전하게 멈추고 재개 시 준비 과정을 처음부터 시작한다.
- 화면 크기가 바뀌어도 구멍이 겹치거나 가로 스크롤이 생기지 않는다.

### F2. 두더지 등장과 판정

게임 중 두더지는 무작위 구멍에 나타나며 동시에 보이는 최대 수는 1단계 1마리, 2단계 2마리, 3단계 3마리다. 사용자가 두더지를 누르면 해당 두더지만 적중으로 처리하고 즉시 다음 등장을 준비한다. 두더지가 사라질 때까지 누르지 못해도 감점하지 않는다. 두더지가 없는 구멍을 직접 누르면 오답으로 처리한다.

완료 기준:

- [ ] 동시에 보이는 두더지는 단계별 최대 수(1·2·3마리)를 넘지 않는다.
- [ ] 같은 구멍만 반복하지 않도록 가능한 구멍 중 무작위로 등장한다.
- [ ] 보이는 두더지를 한 번 누르면 한 번만 적중 처리된다.
- [ ] 두더지를 놓친 경우 점수는 변하지 않는다.
- [ ] 빈 구멍을 누르면 한 번만 오답 처리된다.
- [ ] 두더지가 바뀌는 순간의 빠른 연속 입력으로 중복 점수가 생기지 않는다.

예외 상황:

- 두더지 표시 시간이 끝나는 순간과 터치가 겹쳐도 적중 또는 놓침 중 하나만 처리한다.
- 보드 밖 터치, 상태 표시, 버튼 터치는 오답으로 처리하지 않는다.

### F3. 점수와 단계 상승

두더지 적중은 100점을 더하고 빈 구멍 오답은 50점을 뺀다. 점수는 0 아래로 내려가지 않는다. 누적 10마리 적중마다 다음 단계로 올라가며 판은 3×3, 4×4, 5×5 순서로 커진다. 5×5 이후에는 크기를 유지한다. 단계가 올라갈수록 두더지가 머무는 시간을 줄이되 사람이 반응할 수 있는 최소 시간을 둔다.

완료 기준:

- [ ] 적중 한 번마다 점수가 정확히 100점 증가한다.
- [ ] 빈 구멍 오답 한 번마다 점수가 정확히 50점 감소한다.
- [ ] 0점에서 오답을 눌러도 점수는 0점이다.
- [ ] 10마리째 적중 후 2단계 4×4로 바뀐다.
- [ ] 20마리째 적중 후 3단계 5×5로 바뀐다.
- [ ] 30마리 이후에도 5×5를 유지하며 속도 난도는 계속 조정할 수 있다.
- [ ] 단계 변경 중 입력으로 점수나 적중 수가 중복되지 않는다.

예외 상황:

- 판 크기가 바뀔 때 이전 판의 두더지와 예약 작업을 제거한다.
- 매우 빠른 기기와 느린 기기 모두 실제 경과 시간을 기준으로 두더지를 교체한다.

### F4. 시간과 결과

카운트다운이 끝나면 60초 타이머가 시작된다. 남은 시간이 0이 되면 즉시 두더지와 입력을 비활성화하고 결과 화면을 한 번만 연다. 결과에는 이번 점수, 최고 점수, 도달 단계, 잡은 두더지 수를 표시한다.

완료 기준:

- [ ] 타이머는 실제 경과 시간에 따라 줄고 0 아래로 내려가지 않는다.
- [ ] 시간이 끝난 후 추가 점수나 감점이 발생하지 않는다.
- [ ] 결과 화면은 한 판에 한 번만 표시된다.
- [ ] 결과의 네 기록이 실제 플레이 기록과 일치한다.
- [ ] `다시 하기`를 누르면 점수·단계·적중 수·시간·두더지가 초기화된다.

예외 상황:

- 종료 시점과 터치가 겹치면 종료 전에 발생한 유효 입력만 한 번 반영한다.
- 여러 번 빠르게 다시 하기를 눌러도 타이머가 중복 실행되지 않는다.

### F5. 타격 피드백과 기기 저장

모든 게임판 터치에는 사용자가 누른 구멍을 향해 망치가 내려치는 짧은 애니메이션과 짧은 진동을 제공한다. 두더지를 잡았을 때는 일반 터치보다 분명히 긴 진동, 타격 효과음, 별과 충격파가 터지는 성공 애니메이션을 함께 제공한다. 빈 구멍 오답에도 망치는 내려치지만 성공 효과와 혼동되지 않는 짧은 실패 효과를 사용한다. 빠르게 터치해도 각 망치 효과는 누른 위치에 표시하되 게임 판정은 중복되지 않아야 한다. 음소거 버튼은 효과음만 끄며 진동은 기기 설정을 따른다. 현재 기기의 브라우저 저장소에 최고 점수와 최고 단계를 저장한다.

완료 기준:

- [ ] 구멍을 누르면 짧은 진동이 한 번 발생한다.
- [ ] 구멍을 누를 때마다 해당 위치에 망치가 내려치는 애니메이션이 보인다.
- [ ] 적중하면 일반 터치보다 긴 진동이 발생한다.
- [ ] 적중 순간 별·충격파 등 성공을 분명히 알 수 있는 시각 효과가 보인다.
- [ ] 빈 구멍 오답에는 망치 효과가 보이지만 적중용 별·충격파는 나타나지 않는다.
- [ ] 망치와 적중 효과가 다음 두더지를 가리거나 터치 판정을 막지 않는다.
- [ ] 진동 미지원·거부 환경에서도 오류 없이 게임이 진행된다.
- [ ] 적중과 오답 효과음을 구분할 수 있고 음소거 상태에서는 소리가 나지 않는다.
- [ ] 새 최고 점수와 최고 단계만 기존 기록을 갱신한다.
- [ ] 새로고침 후에도 현재 기기에서 최고 기록과 음소거 설정이 유지된다.

예외 상황:

- 브라우저 저장이 차단되거나 데이터가 깨졌으면 기본 기록 0으로 시작하고 게임은 계속한다.
- 저장 실패 시 짧은 안내를 한 번 표시하며 긴 오류 내용으로 게임판을 가리지 않는다.
- 인터넷 연결이 끊겨도 이미 열린 게임은 끝까지 진행된다.

### F6. 안전한 일시정지

게임 중 다른 탭으로 이동하거나 창이 가려지면 시간, 두더지 등장, 입력을 즉시 멈추고 게임판을 가린다. 사용자가 `계속하기`를 눌러야 재개한다.

완료 기준:

- [ ] 일시정지 중에는 시간과 두더지 표시 시간이 흐르지 않는다.
- [ ] 일시정지 화면 뒤의 두더지를 볼 수 없고 누를 수도 없다.
- [ ] 재개 후 점수·단계·적중 수·남은 시간이 이어진다.
- [ ] 일시정지와 재개를 반복해도 타이머나 등장 작업이 중복되지 않는다.

예외 상황:

- 초기 카운트다운 중 중단되면 재개 후 카운트다운을 처음부터 다시 시작한다.
- 결과 화면에서는 자동 일시정지를 열지 않는다.

## 6. 이번에 만들지 않는 것과 이유

- 로그인과 서버 저장: 한 기기에서 즉시 플레이하는 핵심 재미에 필요하지 않다.
- 온라인 순위와 공유: 서버 운영과 개인정보 처리가 필요해 첫 버전 범위를 넘는다.
- 특수 두더지와 아이템: 기본 터치 판정과 단계 난도를 먼저 검증한다.
- 결제, 광고, 알림: 게임의 핵심 흐름과 관련이 없다.
- 기록 삭제와 복원: 저장 정보가 최고 기록과 음소거뿐이므로 첫 버전에서는 제외한다.

## 7. 다뤄야 하는 정보

- 게임 상태: 준비, 진행, 일시정지, 종료
- 한 판 기록: 현재 점수, 남은 시간, 단계, 누적 적중 수
- 판 상태: 행·열 크기, 현재 두더지 위치, 두더지 등장·종료 시각, 입력 잠금 여부
- 기기 저장: 최고 점수, 최고 단계, 음소거 여부, 저장 형식 버전
- 사용자 이름, 연락처, 위치 등 개인정보는 수집하지 않는다.

## 8. 반드시 지켜야 할 것

- 320px 너비 모바일 세로 화면에서 주요 정보와 게임판이 잘리지 않아야 한다.
- 모든 게임판 터치와 적중 진동의 길이가 분명히 달라야 한다.
- 망치 애니메이션은 실제로 누른 구멍에 나타나고 적중 시점과 맞아야 한다.
- 색상만으로 적중과 오답을 구분하지 않고 모양과 움직임도 함께 사용해야 한다.
- 적중·오답·놓침을 한 입력에서 중복 판정하지 않아야 한다.
- 단계 변경 시 이전 두더지와 타이머 작업을 안전하게 정리해야 한다.
- 진동, 소리, 저장 기능 실패가 게임 진행을 막아서는 안 된다.
- 키보드 사용자와 모션 감소 설정도 기본 조작과 정보를 이용할 수 있어야 한다.

## 9. 절대 하지 말아야 할 것

- 놓친 두더지에 점수를 차감하지 않는다.
- 점수를 0 아래로 내리지 않는다.
- 보드 밖이나 게임 버튼 터치를 오답으로 처리하지 않는다.
- 한 번의 적중을 두 번 이상 집계하지 않는다.
- 숨겨진 탭에서 제한 시간이나 두더지 등장 시간을 계속 흘려보내지 않는다.
- 진동이나 로컬 저장소를 사용할 수 없다는 이유로 게임을 종료하지 않는다.

## 10. 제약사항

- 플랫폼: 모바일 우선 반응형 웹, PC 브라우저 지원
- 운영: 설치·로그인·별도 서버 없이 정적 웹으로 배포
- 기술: HTML, CSS, JavaScript, 브라우저 진동 API와 오디오 기능
- 배포: GitHub Pages
- 저장: 현재 브라우저의 로컬 저장소만 사용
- 비용: 유료 서비스와 유료 소재를 사용하지 않음
- 접근 범위: 공개 링크를 아는 누구나 각자 한 기기에서 플레이
- 상시 가동: GitHub Pages에서 상시 접근 가능

## 11. 성공 판단 기준

- 처음 보는 사용자가 설명 없이 게임을 시작해 두더지를 잡고 오답 감점을 이해할 수 있다.
- 60초 한 판을 진행 불가나 중복 판정 없이 끝낼 수 있다.
- 10마리와 20마리 적중 시 판 크기와 난도 변화가 눈에 띈다.
- 무작위 연타보다 두더지를 보고 정확히 누르는 플레이가 더 높은 점수를 얻는다.
- 모바일에서 일반 터치와 적중의 진동 차이를 느낄 수 있다.

## 12. 가정 및 확인 필요

- 두더지는 1단계 최대 1마리, 2단계 최대 2마리, 3단계 최대 3마리가 동시에 나타나는 것으로 확정했다.
- 단계별 정확한 등장 간격과 표시 시간은 첫 구현에서 사람이 반응 가능한 값으로 정한 뒤 플레이 테스트로 조정한다.
- 모든 터치는 약한 진동, 적중은 더 긴 진동으로 구현하되 실제 세기는 기기와 브라우저에 따라 달라질 수 있다.
- 망치는 CSS 또는 간단한 자체 그래픽으로 만들고, 약 0.2~0.3초 안에 내려쳤다가 사라지게 한다.
- 적중 시각 효과는 별과 원형 충격파를 기본안으로 사용하며 모션 감소 설정에서는 움직임을 줄인 정적 강조로 대체한다.
- 5×5 이후에는 판 크기를 더 키우지 않고 속도만 조정할 수 있는 구조로 만든다.
- 깔끔하고 밝은 코믹 아케이드 스타일은 자체 CSS 도형과 문자 기반 시각 요소로 먼저 구현한다.

## 13. 아직 안 정한 것

- 단계별 두더지 표시 시간과 다음 등장 사이의 정확한 수치
- 적중·오답에 사용할 최종 효과음 음색
- 5×5 이후 속도 난도의 최대 단계와 하한값
